// import words from "./words.json"
// import {wordsArr} from '../components/game-components/Game'

// export const generate = (wordsArr) => {
//   console.log('words.js',wordsArr)
//   if(wordsArr && wordsArr.length){
//   var random = wordsArr[Math.floor(Math.random()*wordsArr.length)]
//   console.log('random', random)
//   return (random.word)
// // }else {
    // var random = words[Math.floor(Math.random()*words.length)]
    // return (random && random.word)
//   }
// };

// const getRandomWord = (data) => { let chars = getRandomWord([Math.floor(Math.random() * data.length)]); 